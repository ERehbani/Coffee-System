spring:
  application:
    name: Coffee-System
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/coffee_db}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
