spring:
  application:
    name: Coffee-System
    datasource:
      url: jdbc:mysql://localhost:3306/test
      username: ${DB_USER}
      password: 123
      driver-class-name: com.mysql.cj.jdbc.Driver

      jpa:
        hibernate:
          ddl-auto: update
        show-sql: true
        properties:
          hibernate:
            format_sql: true